package fr.vinarnt.animu.finder.compose.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes

private val shape = RoundedCornerShape(0)

val AppShape = Shapes(
    extraSmall = shape,
    small = shape,
    medium = shape,
    large = shape,
    extraLarge = shape
)
